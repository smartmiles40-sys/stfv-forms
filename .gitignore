node_modules
dist
dist-ssr
.vercel
*.local
.DS_Store

# saída do `npm run verificar` (amostras do código gerado)
.amostra
.amostra-runner.mjs
.gerador-check.mjs

# rascunhos de uma sessão (publicar um form na mão, gerar spec, etc.)
.tmp-*
